[project]
name = "neotorch"
version = "0.1.0"
description = "Layout and tree utilities extracted from the CuTe prototype"
readme = "README.md"
authors = [
    { name = "Patrick Krusiec", email = "patrick.krusiec@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.5,<0.9.0"]
build-backend = "uv_build"
