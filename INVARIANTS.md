# StrideWeave Invariant Registry

This registry records cross-cutting constraints that must shape code before it is
written. Read it while planning a change, not only after implementation. Ordinary
feature behavior belongs in `README.md` and its focused tests; this document covers
rules that apply across modules, backends, or future implementations.

## How to Use This Registry

Before changing code:

1. Identify the affected invariant IDs below.
2. Preserve their canonical implementation choices in the design.
3. Run every listed enforcement mechanism relevant to the change.
4. Update this registry in the same change when adding, removing, or materially
   changing a cross-cutting invariant.

Enforcement labels mean:

- **AST** — enforced by `tools/lint_invariants.py`.
- **Ruff** — enforced by the selected rules in `pyproject.toml`.
- **Test** — enforced behaviorally by the referenced tests.
- **Build** — enforced by compiler, formatter, type checker, or sanitizer jobs.
- **Review** — not reliably provable by current automation; authors and reviewers
  must inspect it explicitly.

The evidence column names stable files and test functions rather than line numbers,
which move frequently.

## Python Source Invariants

| ID | Invariant and canonical form | Enforcement | Evidence |
| --- | --- | --- | --- |
| `SW001` | Coordinate indexing uses `tensor[i, j]`. `tensor[[i, j]]` is reserved for intentional compatibility tests or documentation. | AST | `tools/lint_invariants.py`; `tests/test_lint_invariants.py`; intentional cases in `tests/test_tensor.py` |
| `SW002` | Dtype tags are identity values. Registered descriptors are immutable singletons unique by name, block-scaled descriptors are additionally unique by structure, `Whole` is a true singleton, the `DType` class namespace holds exactly the built-in bindings and is frozen against reassignment, deletion, and any further descriptor attribute while later registrations are reached through `DType.from_name` and `DType.registered`, a rejected construction registers nothing, and copying, pickling, and registry lookup preserve identity. Construction is one transaction owned by the dtype module: the most-derived `__init__` runs first, the complete descriptor is then validated, sealed, and published under one registry lock, so implementations never order registry mutation themselves, a constructor raising at any depth leaves neither its name nor its structure claimed, a concurrent lookup cannot observe an unfinished descriptor, and concurrent constructors cannot produce two identities. A finalization that fails is undone completely, so a rejected object keeps neither structure nor seal. Abstract descriptor classes cannot be constructed, every descriptor is a `DTypeCategory`, a `SimpleDType`, or a `CompoundDType`, and `CompoundDType` owns its plane mapping: it copies the supplied planes into a non-empty tuple of `SimpleDType` descriptors that `simple_types` and `num_carriers` report. A descriptor's stored fields, the accessors that report its representation, and its attribute machinery — `__getattribute__`, `__getattr__`, `__setattr__`, and `__delattr__` — are owned by the model, so a subclass that redefines one — as a slot, property, or method — is refused at class creation, as is any base in its initial hierarchy that supplies one, and so is a later assignment or deletion on any descriptor class; implementations extend the model with their own fields and `structure_extension()`. Ownership is layered by contract class and defined in module state rather than as class attributes, so replacing or deleting class metadata cannot weaken it, while an implementation's own declared slots stay protected against further subclassing. A descriptor's identity is composed by the model rather than reported by the descriptor: each contract class has one immutable module-owned specification carrying the members it owns, the fragment it contributes to a canonical structure, the validation it requires, and whether its descriptors are additionally unique by structure, and finalization applies the specifications a descriptor's class resolves through its method resolution order, general to specific. No identity policy is dispatched through the descriptor, so an implementation cannot omit a canonical layer from its fingerprint, decline the uniqueness its contract imposes, or weaken a check it still claims to satisfy, and the names the model once dispatched through — `_contract_structure`, `_structural_key`, `_structure_conflict`, and `_validate_finalized` — are reserved on the same terms as the accessors. Contracts are not registrable; an external representation inherits a recognized one and stays first-class through registration, subtype and kind queries, discovery, copying, pickling, and `structure_extension()`, which is the sole extension-controlled contribution to representation identity. The same ownership governs a descriptor's own state: an implementation need not declare `__slots__`, but a descriptor whose instance state holds an owned name is rejected by finalization before it is sealed or registered, both as its constructor leaves it and again after finalization callbacks that may assign to it, so a rejected descriptor releases its name and structural key. These are guardrails over cooperative extensions rather than a sandbox, and the enforced part is exactly this: the initial class hierarchy is validated at class creation, the completed descriptor is validated during finalization, registered instances are sealed, and the built-in namespace is frozen, so a registered descriptor's model-owned accessors agree with its recorded structure. The rest is contractual and reviewed rather than enforced: from the moment a descriptor class is created, that extension class and every base contributing behavior to it must stay stable; representation-bearing state must be initialized before registration and described by a pure, stable `structure_extension()`; and objects an extension merely refers to are not deep-frozen. Custom `__dict__` concealment, reassigning `__slots__`, mutating an extension class or participating mixin after descriptor class creation, `object.__setattr__`, `type.__setattr__`, and private-module mutation are unsupported and forfeit these guarantees rather than being intercepted. Pickling resolves a name and structure against the receiving process's registry instead of rebuilding a descriptor, so built-ins load wherever `strideweave` is imported, a dynamically registered dtype requires the receiving process to have registered a matching descriptor first, and a missing or differently defined registration raises rather than substituting another descriptor. A structure describes the complete referenced graph — each contract class's own fields plus the recursively expanded structure of every supertype, plane, element, and scale it names, with implementation state declared through `structure_extension()` — accepts only exact strings, numbers, `None`, `Whole`, and tuples of those and stores every leaf as a string naming its exact type alongside its value, so comparison is type-exact rather than numeric and `NaN` has one deterministic spelling, and may reference only canonically registered descriptors — finalized and still the descriptor their name resolves to — which keeps it acyclic and keeps a rejected object out of every graph. Compare `DType.*` and zero-argument `.dtype()` results with `is` or `is not`, never `==` or `!=`. | AST, Test, Review | `tools/lint_invariants.py`; `tests/test_lint_invariants.py`; `tests/test_dtype.py::test_registry_lookup_by_name_is_identity_safe`; `tests/test_dtype.py::test_copying_and_pickling_preserve_identity`; `tests/test_dtype.py::test_built_in_dtypes_unpickle_in_a_fresh_process`; `tests/test_dtype.py::test_extension_dtype_unpickles_where_it_is_registered_first`; `tests/test_dtype.py::test_extension_dtype_unpickling_requires_a_registration`; `tests/test_dtype.py::test_unpickling_never_substitutes_a_differently_defined_dtype`; `tests/test_dtype.py::test_unpickling_checks_every_referenced_descriptor`; `tests/test_dtype.py::test_a_matching_referenced_graph_still_resolves_to_the_receiver_identity`; `tests/test_dtype.py::test_a_pickle_carries_only_a_name_and_a_structure`; `tests/test_dtype.py::test_a_structure_extension_distinguishes_otherwise_equal_representations`; `tests/test_dtype.py::test_a_structure_extension_can_claim_a_distinct_block_scaled_representation`; `tests/test_dtype.py::test_a_structure_must_hold_only_deterministic_immutable_values`; `tests/test_dtype.py::test_numerically_equal_tags_of_different_types_stay_distinct`; `tests/test_dtype.py::test_a_sender_tag_cannot_reconstruct_against_a_differently_typed_receiver_tag`; `tests/test_dtype.py::test_finite_and_infinite_float_tags_reconstruct_exactly`; `tests/test_dtype.py::test_a_nan_tag_is_canonical_rather_than_non_reflexive`; `tests/test_dtype.py::test_every_structure_leaf_is_an_encoded_string`; `tests/test_dtype.py::test_a_structure_cannot_reference_an_unfinalized_descriptor`; `tests/test_dtype.py::test_descriptors_are_immutable`; `tests/test_dtype.py::test_block_scaled_descriptors_are_unique_by_structure`; `tests/test_dtype.py::test_whole_extent_is_a_true_singleton`; `tests/test_dtype.py::test_built_in_bindings_reject_reassignment_and_deletion`; `tests/test_dtype.py::test_the_built_in_namespace_admits_no_further_descriptor`; `tests/test_dtype.py::test_namespace_protection_cannot_be_disabled_by_class_assignment`; `tests/test_dtype.py::test_failed_registration_reserves_neither_name_nor_structure`; `tests/test_dtype.py::test_a_failed_commit_releases_the_structure_it_had_already_claimed`; `tests/test_dtype.py::test_registry_keys_are_exact_strings_that_cannot_run_user_code`; `tests/test_dtype.py::test_abstract_descriptor_classes_cannot_be_constructed`; `tests/test_dtype.py::test_a_descriptor_must_be_one_of_the_three_descriptor_kinds`; `tests/test_dtype.py::test_a_descriptor_implementation_must_initialize_its_base`; `tests/test_dtype.py::test_an_external_compound_extension_registers_through_public_apis_only`; `tests/test_dtype.py::test_an_external_compound_extension_unpickles_where_it_is_registered_first`; `tests/test_dtype.py::test_a_constructor_failing_after_its_base_registers_nothing`; `tests/test_dtype.py::test_compound_planes_are_validated_before_any_registry_mutation`; `tests/test_dtype.py::test_compound_planes_are_copied_out_of_the_callers_collection`; `tests/test_dtype.py::test_mutating_the_supplied_collection_cannot_change_a_registered_descriptor`; `tests/test_dtype.py::test_a_compound_subclass_cannot_redefine_the_owned_plane_accessors`; `tests/test_dtype.py::test_a_subclass_cannot_shadow_the_state_the_model_owns`; `tests/test_dtype.py::test_owned_state_cannot_be_patched_onto_a_class_after_it_is_created`; `tests/test_dtype.py::test_legacy_ownership_metadata_cannot_disable_the_policy`; `tests/test_dtype.py::test_ownership_is_layered_over_the_contract_classes`; `tests/test_dtype.py::test_an_extension_cannot_own_the_identity_policy_hooks`; `tests/test_dtype.py::test_an_extension_cannot_omit_its_contract_layers_from_its_fingerprint`; `tests/test_dtype.py::test_an_extension_cannot_decline_the_uniqueness_its_contract_imposes`; `tests/test_dtype.py::test_an_extension_cannot_weaken_the_validation_it_still_claims`; `tests/test_dtype.py::test_contract_specifications_cannot_be_weakened_by_class_assignment`; `tests/test_dtype.py::test_the_canonical_structure_layers_follow_the_contract_mro`; `tests/test_dtype.py::test_an_extension_keeps_owning_the_fields_it_declares`; `tests/test_dtype.py::test_a_descriptor_cannot_assign_an_owned_accessor_on_itself`; `tests/test_dtype.py::test_root_owned_state_reaching_the_instance_dictionary_is_refused`; `tests/test_dtype.py::test_layer_owned_state_reaching_the_instance_dictionary_is_refused`; `tests/test_dtype.py::test_a_rejected_shadowing_descriptor_keeps_no_finalization_state`; `tests/test_dtype.py::test_a_shadow_introduced_during_finalization_is_rejected`; `tests/test_dtype.py::test_a_late_shadow_leaves_the_structural_key_reusable`; `tests/test_dtype.py::test_a_dictionary_backed_extension_keeps_its_own_fields`; `tests/test_dtype.py::test_a_descriptor_class_cannot_intercept_attribute_access`; `tests/test_dtype.py::test_a_descriptor_class_cannot_inherit_owned_members_from_a_mixin`; `tests/test_dtype.py::test_an_inherited_owned_member_is_refused_at_the_layer_that_owns_it`; `tests/test_dtype.py::test_a_passive_mixin_and_its_own_api_stay_supported`; `tests/test_dtype.py::test_an_extension_may_still_define_its_own_properties_and_methods`; `tests/test_dtype.py::test_the_supported_extension_immutability_boundary_holds_end_to_end`; `tests/test_dtype.py::test_a_compound_subclass_must_supply_its_planes_to_the_base`; `tests/test_dtype.py::test_a_rejected_registration_cannot_be_referenced_by_a_later_descriptor`; `tests/test_dtype.py::test_a_descriptor_rejected_for_its_structure_keeps_no_finalization_state`; `tests/test_dtype.py::test_a_block_scaled_subclass_is_not_a_second_identity_for_a_representation`; `tests/test_dtype.py::test_a_partly_constructed_descriptor_is_never_reachable`; `tests/test_dtype.py::test_a_failed_structure_commit_leaves_the_name_unclaimed`; `tests/test_dtype.py::test_registration_commits_inside_the_shared_registry_lock`; `tests/test_dtype.py::test_concurrent_same_name_registration_yields_exactly_one_descriptor`; `tests/test_dtype.py::test_concurrent_same_structure_registration_claims_one_structure` |
| `SW003` | `Carrier.is_mutable()` owns the public mutability policy. Python carrier subclasses implement only the `_is_mutable()` backend hook. | AST, Test | `tools/lint_invariants.py`; `tests/test_lint_invariants.py`; carrier mutability tests |
| `SW004` | Tensor logic uses `tensor.size()` for logical element count. Use `layout.size` only when the code genuinely has a layout rather than a tensor or parameter. | AST | `tools/lint_invariants.py`; `tests/test_lint_invariants.py` |
| `PY001` | Every multi-input `zip` call states truncation intent with `strict=True` or `strict=False`. | Ruff (`B905`) | `pyproject.toml` |
| `PY002` | Re-export modules use ordinary imports and sorted literal `__all__` declarations. Wildcard imports remain only in compatibility aggregators whose exports are dynamically composed. | Ruff (`RUF022`, `PLC0414`), Test | `pyproject.toml`; `src/strideweave/operation.py`; `tests/test_docstrings.py::test_operation_runtime_exports_match_stub_declarations` |
| `PY003` | Broad exception assertions name the expected message, contain one simple statement, do not catch `Exception`, and make regular-expression intent explicit. | Ruff (`PT011`, `PT012`, `B017`, `RUF043`) | `pyproject.toml`; test suite |
| `PY004` | Stub declarations use current typing syntax and valid stub defaults rather than executable default expressions. | Ruff (`PYI011`, `PYI026`, `PYI034`, `PYI035`, `UP035`) | `pyproject.toml`; `.pyi` files |

### AST Checker Operation

Run:

```bash
uv run python tools/lint_invariants.py
```

With no arguments it scans `src`, `tests`, and `examples` without importing the
package. It parses each file once, ignores virtual environments, caches, build output,
and recognized generated Python files, and emits deterministic
`path:line:column: SWxxx message` diagnostics.

An intentional exception must be attached to the exact affected line:

```python
tensor[[i, j]]  # strideweave-lint: ignore=SW001
```

Multiple named codes may be comma-separated. Do not use broad file-level suppression.
Exit status is `0` when clean, `1` when diagnostics are present, and `2` for invalid
input, unreadable files, or syntax errors.

## Runtime and API Invariants

| ID | Invariant and canonical form | Enforcement | Evidence |
| --- | --- | --- | --- |
| `RT001` | Operation dispatch is invoked on a carrier instance and follows the documented exact carrier class. `Carrier.dispatch_op` owns shared policy and metadata annotation, and rejects an already-dispatched operation; backends override only `_dispatch_op`, whose factories return fresh operation instances. `Carrier` is the open extension interface, and the shipped concrete implementations `Generic`, `CPU`, `FileBacked`, and `Evictable` are closed: each refuses subclass creation with one shared message naming composition and operation lowering as the supported alternative, so its allocation factories, storage normalization, dispatch metadata, and capability declarations always describe its exact class — its exact instance, for the dependent `Evictable` — and no factory needs an exact-class guard. Every stub declaring one of those classes marks it `@final`, including the facade a user imports and the native declaration behind it, so the same closure is reported statically rather than only at run time. A new backend is a sibling `Carrier` that owns the carriers it delegates to. Its `_dispatch_op` returns a top-level operation owned by that sibling, never a raw operation from an owned carrier; delegated operations are wrapped in an adapter that preserves the composing carrier's exact class in dispatch metadata, profiler filtering, results, gradients, and capability declarations. | AST, Test | `tools/lint_invariants.py`; `tests/test_lint_invariants.py`; `tests/test_carrier.py::test_a_concrete_carrier_cannot_be_subclassed`; `tests/test_carrier.py::test_a_closed_carrier_states_one_refusal`; `tests/test_carrier.py::test_a_closed_carrier_is_final_on_its_public_import_path`; `tests/test_carrier.py::test_every_stub_declaring_a_closed_carrier_marks_it_final`; `tests/test_carrier.py::test_the_carrier_base_stays_open_to_implementation`; `tests/test_cpu.py::test_cpu_is_a_closed_carrier_implementation`; `tests/test_cpu.py::test_a_composed_carrier_lowers_standard_operations_onto_cpu`; `tests/test_generic_reference.py::test_generic_is_a_closed_carrier_implementation`; fresh and cached-operation dispatch tests in `tests/test_carrier.py`, `tests/test_cpu.py`, and `tests/test_evictable.py` |
| `RT002` | Tensor and operation-result storage covers `layout.cosize`, not merely logical `layout.size`. Logical iteration still uses `tensor.size()`. | Test | `tests/test_tensor.py::test_tensor_storage_validation_uses_cosize_not_logical_size`; hierarchical CPU operation tests; factory allocation tests |
| `RT003` | Every successful public mutation path changes the visible carrier version. Aliases and views observe the same version; storage-only transitions such as eviction do not pretend to be value mutations. | Test | `tests/test_carrier.py::test_generic_public_write_paths_increment_version`; carrier scatter tests; `tests/test_cpu.py::test_cpu_data_contract_and_mutation`; `tests/test_tensor.py::test_tensor_version_is_shared_by_views_and_same_data_tensors`; evictable version tests |
| `RT004` | Carrier factories preserve dtype unless explicitly overridden, honor their public mutability argument, and allocate fresh storage of the requested physical size. `allocate_like(..., empty=False)` provides initialized storage; `empty=True` permits unspecified initial values and CPU skips value initialization. | Test, Review | Generic, CPU, FileBacked, Evictable `new_like` and `allocate_like` tests in `tests/test_carrier.py`, `tests/test_cpu.py`, `tests/test_file_backed.py`, and `tests/test_evictable.py`; native CPU allocation review |
| `RT005` | Public Python exports satisfy the repository docstring contract. Layout-command APIs additionally document StrideWeave syntax, semantics, and tensor mode assumptions. | Test | `AGENTS.md`; `tests/test_docstrings.py` |
| `RT006` | Runtime public exports and their `.pyi` declarations stay aligned; pybind signatures and defaults must agree with the stubs. | Test, Build, Review | `tests/test_docstrings.py::test_operation_runtime_exports_match_stub_declarations`; `uv run pyright`; relevant `.pyi` and binding files |
| `RT007` | Saved autograd inputs retain their carrier version, and backward rejects mutation that would invalidate the saved forward state. | Test | tensor mutation-after-forward tests in `tests/test_tensor.py` and `tests/test_evictable.py` |
| `RT008` | Carrier ownership is exclusive: owned tiers cannot be mutated, released, or moved through external aliases, while mutation through the owning carrier remains available. | Test | ownership and nested hierarchy tests in `tests/test_evictable.py` |
| `RT009` | Moving storage preserves tensor layout, dtype, values, and autograd semantics; failures leave the source usable and do not silently release it. | Test | `tests/test_move.py` |
| `RT010` | DLPack exports preserve dtype, offset, and flattened hierarchical shape/strides; unsupported carriers and requests fail explicitly. | Test | `tests/test_dlpack.py` |
| `RT011` | Composite adapters own the visible autograd graph and the translation boundary between outer and inner carriers. A composite carrier never calls another carrier's protected `_dispatch_op` or returns its public `dispatch_op` result directly: its own `_dispatch_op` wraps a normally dispatched nested operation in a composite-owned adapter. That adapter requires any plan the central policy resolves for its outer operands against the composing carrier's own capabilities before it lowers anything, converts every tensor argument into a representation accepted by the nested operation, delegates computation and internal storage transitions through the framework-owned sealed `execute_lowered_operation` helper, and wraps tensor results and gradients back into the composite representation before they cross its boundary. It never dynamically dispatches another operation's `forward`, `_forward`, or `_execute_lowered` attribute; lowered execution shares framework hooks and Tensor-result validation without attaching an inner autograd node or clearing delegated backward state. | AST, Test, Review | `tools/lint_invariants.py`; `tests/test_lint_invariants.py`; `tests/test_cpu.py::test_a_composed_carrier_lowers_standard_operations_onto_cpu`; lowered-execution tests in `tests/test_operation.py` and `tests/test_evictable.py`; Evictable transition tests |
| `RT012` | A carrier stores exactly one dtype drawn from its documented accepted set, matched by identity: `Generic` accepts `Any`, `Floating`, `Float32`, and `Int32`, `CPU` accepts `Float32` and `Int32`, `FileBacked` accepts `Floating`, `Float32`, and `Int32`, and `Evictable` accepts what both composed tiers accept. `Any` and `Floating` are accepted only as those carriers' legacy opaque storage, and a carrier accepting a concrete simple dtype stores it normalized and owned rather than as arbitrary objects; every other abstract category, every simple encoding outside the accepted set, and every compound descriptor is rejected at construction rather than partially stored, and every carrier reports deferred compound storage with the same diagnostic. Membership is decided by object identity and never by equality, so an object with a spoofed, raising, or side-effecting `__eq__` is rejected like any other unsupported dtype. Python carriers share `validate_storage_dtype`; the native CPU carrier mirrors it in `parse_cpu_dtype`. The same accepted set is readable without allocating through `Carrier.supports_storage_dtype`, a structural, side-effect-free query about what the implementation can allocate: it is independent of size, mutability, ownership, eviction residency, release, and the dtype the instance currently holds, matches descriptors by identity, treats every unsupported descriptor — abstract category, unimplemented narrow encoding, or deferred compound format — as unsupported rather than an error, and raises only for a non-`DType` argument. `Evictable` reports the intersection of its tiers. `Carrier` owns that public method and its validation and no carrier overrides it; an implementation states its set through the protected `_supports_storage_dtype` hook, whose conservative default claims only the instance's current dtype. The AST rule reaches a class that names a carrier base directly, which is how every carrier in this repository is written; a carrier reached through an intermediate class is covered by review rather than by lint. | AST, Test, Review | `tests/test_dtype.py::test_each_carrier_accepts_exactly_its_documented_dtype_set`; `tests/test_dtype.py::test_carriers_recognize_storage_dtypes_by_identity_not_equality`; `tests/test_dtype.py::test_a_dtype_lookalike_cannot_impersonate_a_cpu_storage_dtype`; `tests/test_dtype.py::test_cpu_allocation_round_trips_the_canonical_dtype_singletons`; `tests/test_dtype.py::test_legacy_opaque_storage_dtypes_remain_supported`; `tests/test_dtype.py::test_the_opaque_disposition_does_not_extend_any_accepted_set`; `tests/test_dtype.py::test_evictable_composition_preserves_dtype_matching`; `tests/test_dtype.py::test_supported_carrier_construction_remains_compatible`; `tests/test_dtype.py::test_compound_dtypes_are_rejected_before_any_storage_is_built`; `tests/test_dtype.py::test_cpu_rejects_compound_dtypes_before_allocation`; `tests/test_dtype.py::test_every_carrier_explains_deferred_compound_storage_the_same_way`; `tests/test_dtype.py::test_carriers_reject_dtypes_without_storage_support`; `tests/test_dtype.py::test_a_carrier_reports_exactly_the_dtypes_it_can_store`; `tests/test_dtype.py::test_storage_support_agrees_with_what_construction_accepts`; `tests/test_dtype.py::test_no_carrier_claims_a_category_or_encoding_it_cannot_store`; `tests/test_dtype.py::test_the_dtype_a_carrier_currently_holds_does_not_narrow_its_answer`; `tests/test_dtype.py::test_storage_support_is_allocation_free_and_survives_state_changes`; `tests/test_dtype.py::test_an_evictable_hierarchy_reports_what_both_tiers_can_store`; `tests/test_dtype.py::test_an_evictable_answer_is_unchanged_by_residency`; `tests/test_dtype.py::test_a_minimal_custom_carrier_claims_only_what_it_currently_stores`; `tests/test_dtype.py::test_a_custom_carrier_widens_support_through_the_protected_hook`; `tests/test_dtype.py::test_storage_support_requires_a_dtype`; `tests/test_dtype.py::test_a_dtype_lookalike_cannot_claim_storage_support`; `tools/lint_invariants.py`; `tests/test_lint_invariants.py`; `tests/test_dtype.py::test_is_simple_classifies_the_representation_not_carrier_support`; `tests/test_dtype.py::test_validate_storage_dtype_reports_accepted_alternatives`; `src/strideweave/carriers/cpu/native/_cpu_carrier.hpp` |
| `RT013` | Simple-dtype operation policy is central and executable. `strideweave.carriers.operation_policy` is the single source of promotion, per-element arithmetic, accumulation, and result-dtype decisions for `SimpleDType` operands, from which autograd eligibility follows by the framework's existing floating-dtype rule rather than by a duplicated plan field, and `design/SimpleDType-operation-policy.md` is its specification. Plans are immutable, resolved from the module's operation registry so coverage is enumerated rather than sampled, and pinned by explicit expected-plan fixtures rather than values recomputed from the resolver. Unsupported cases have exactly one documented disposition each — legacy opaque categories, abstract categories, compound descriptors, registered but unimplemented simple encodings, non-real weak scalars, and out-of-range weak integers — and none of them falls back to a guessed plan or silently widens an unimplemented dtype. Resolution is value-independent, so a native caller resolves before releasing the GIL. A backend consumes every field of a resolved plan as the source of its execution behavior — operand conversion from `convert_to`, term combination from `accumulation`, result storage from `output` — and never re-derives one field from another or defines local promotion or result-dtype policy; a backend that cannot execute a plan raises rather than substituting one it can. Which resolved plan shapes a backend can execute is recorded in `strideweave.carriers.operation_capability`, one registry whose stored entries serve both execution acceptance and capability introspection, so no informational support table can drift from the executed one. A capability is an exact plan shape and decides nothing: it never selects a promotion, rewrites a plan, or substitutes a nearby supported shape. Matching is exact in operation name, operand roles, source dtypes, conversion targets, compute, accumulation — including its presence or absence, which is operation-specific — and output. Entries are immutable, are enumerated in a deterministic order derived from names rather than registration order, and a class declaring one shape twice is rejected; capabilities are owned by an exact carrier class and resolved for that class alone, with no traversal of its bases, so a class that declares nothing supports nothing and no declaration can widen another class's set. Every shipped independent carrier class declares its entries exactly once during carrier-package initialization and is sealed against further declarations, including the storage carrier that intentionally declares the empty set, so no shipped independent class is reachable in an undeclared state whatever is imported first; the shipped dependent carrier `Evictable` is deliberately absent from that bootstrap and finalizes per instance instead; that declaration path is framework-internal, is exported by no module, and validates its class exactly as public registration does. Every independent class's set is sealed before anyone can rely on it: a declaration is complete and one-shot, publishing the whole validated set and closing the class in one lock transaction, and observing an independent class that has not declared atomically seals its empty set, so first observation is the final answer, an empty declaration closes the class as firmly as a populated one, and a rejected declaration publishes nothing and leaves the class open. There is no public seal or finalize call. Public registration accepts only an independent `Carrier` implementation and rejects `object`, the `Carrier` root, an unrelated class, a `DependentCarrier` class, and every sealed built-in, distinguishing a shipped backend from a custom class that has already declared or been observed with its own stable diagnostic, so a carrier composing a shipped backend declares for its own exact class the shapes it executes. Exact-class ownership is the independent-carrier model and the default; `DependentCarrier` is the second extension category, for a carrier whose executable plans depend on the carrier instances it composes, and it stays open to subclassing. A dependent carrier owns its capabilities per constructed instance and can declare none for its class, through either declaration path. Its generation algorithm is the concrete carrier's own and is unimplemented by the base, which never calls it: construction calls the explicit `_finalize_dependent_capabilities()` step as its last act, once its dependencies are valid, and that step materializes the generated entries exactly once, requires `OperationCapability` entries, rejects one exact shape generated twice, and publishes a deterministically ordered immutable per-instance snapshot. An instance answers no capability query before it finalizes and cannot finalize twice, a failed generation or validation publishes nothing rather than a partial snapshot, and snapshots are owned by instance identity, so two instances of one dependent class — including instances that compare equal — answer independently. A plan matching no capability raises `UnsupportedOperationPlan` once, before allocation or kernel execution. The same stored entries answer a caller: `Carrier.operation_capabilities`, `supports_operation_plan`, `unsupported_plan_reason`, and `require_operation_plan` are asked of a carrier instance and resolve generically to whichever set owns it — the exact class's sealed declarations for an independent carrier, that instance's frozen snapshot for a `DependentCarrier`, with no MRO traversal and no carrier-class branch — so they describe the carrier's capabilities rather than its own storage dtype, return immutable descriptors and no executor or mutable registry, and distinguish an unsupported operation from an unsupported plan shape, and an advertised plan is executable and an executed plan was advertised. A carrier class never overrides those four public methods; a backend states its reach by declaring for its class or by generating a dependent instance's snapshot. The AST rule enforcing that reaches a class naming a carrier base directly, as every carrier here does, and review covers a carrier reached through an intermediate class. `Evictable` is that dependent backend: at the end of successful construction it freezes exactly the plans its own primary instance advertises whose output dtype both tiers structurally support, without rewriting plans, choosing promotion, inspecting carrier classes, or keeping an operation table of its own, and a nested dependent primary composes through its instance snapshot; a generation failure relinquishes both tiers' ownership and publishes no usable hierarchy. `EvictableOperation` resolves the logical plan the central policy gives for its operation name and its outer operands and requires it against that same instance snapshot before any lowering, nested allocation, or kernel work, so every executed planned simple-dtype operation was advertised by that outer instance. Only an unregistered operation name and legacy opaque operand storage bypass planning; a registered operation given operands its shape does not accept is refused by the central resolver at that gate rather than diagnosed later by the primary operation that received them. The frozen snapshot is structural and unchanged by eviction, promotion, or release. `Generic` is the reference backend, and native `CPU` resolves each plan through the same module and requires a matching capability while holding the GIL, before it allocates a result or releases the GIL for the kernel loop; the native bridge lowers the accepted capability to its kernel enums rather than maintaining a mirror of the registry. Revising the policy changes the specification, the resolver, its fixtures, and every backend conformance expectation in one change. | AST, Test, Review | `tests/test_operation_policy.py::test_every_registered_operation_has_a_fixture`; `tests/test_operation_policy.py::test_resolved_plans_match_their_expected_fixtures`; `tests/test_operation_policy.py::test_every_supported_tensor_dtype_combination_is_planned`; `tests/test_operation_policy.py::test_plans_are_immutable_and_hashable`; `tests/test_operation_policy.py::test_an_unimplemented_dtype_is_never_silently_widened`; `tests/test_operation_policy.py::test_legacy_opaque_categories_are_not_simple_promotion`; `tests/test_operation_policy.py::test_a_compound_operand_reports_the_deferred_capability`; `tests/test_operation_capability.py::test_a_registered_capability_is_enumerated_and_matched`; `tests/test_operation_capability.py::test_enumeration_is_deterministic_rather_than_registration_ordered`; `tests/test_operation_capability.py::test_enumeration_returns_an_immutable_view_of_immutable_entries`; `tests/test_operation_capability.py::test_a_class_declaring_one_shape_twice_is_rejected`; `tests/test_operation_capability.py::test_a_second_declaration_is_refused_and_changes_nothing`; `tests/test_operation_capability.py::test_an_empty_declaration_is_complete_and_closes_the_class`; `tests/test_operation_capability.py::test_a_rejected_declaration_leaves_the_class_open`; `tests/test_operation_capability.py::test_observing_an_undeclared_class_seals_its_empty_set`; `tests/test_operation_capability.py::test_requiring_a_plan_seals_an_undeclared_class`; `tests/test_operation_capability.py::test_a_declaration_before_any_observation_is_the_answer`; `tests/test_operation_capability.py::test_a_closed_class_says_why_it_is_closed`; `tests/test_operation_capability.py::test_a_declaration_racing_observers_is_all_or_nothing`; `tests/test_operation_capability.py::test_a_plan_differing_in_one_field_does_not_match`; `tests/test_operation_capability.py::test_a_declared_accumulation_is_not_satisfied_by_a_plan_without_one`; `tests/test_operation_capability.py::test_an_undeclared_backend_supports_nothing_the_policy_resolves`; `tests/test_operation_capability.py::test_an_unmatched_plan_is_never_answered_with_a_nearby_capability`; `tests/test_operation_capability.py::test_a_declaration_belongs_to_the_exact_class_that_made_it`; `tests/test_operation_capability.py::test_a_declaration_never_widens_another_class`; `tests/test_operation_capability.py::test_a_late_declaration_reaches_only_the_class_it_names`; `tests/test_operation_capability.py::test_sibling_classes_stay_isolated`; `tests/test_operation_capability.py::test_only_an_independent_carrier_implementation_may_declare`; `tests/test_operation_capability.py::test_a_shipped_backends_declarations_are_sealed`; `tests/test_operation_capability.py::test_a_storage_carrier_declares_and_seals_an_empty_set`; `tests/test_operation_capability.py::test_no_exported_declaration_path_reaches_a_shipped_backend`; `tests/test_operation_capability.py::test_the_internal_declaration_path_is_not_a_wider_escape_hatch`; `tests/test_operation_capability.py::test_reinitializing_the_built_in_capabilities_changes_nothing`; `tests/test_operation_capability.py::test_every_shipped_backend_is_sealed_whatever_is_imported_first`; `tests/test_operation_capability.py::test_a_shipped_backend_declares_exactly_what_it_executes`; `tests/test_operation_capability.py::test_a_dependent_instance_answers_with_what_it_generated`; `tests/test_operation_capability.py::test_two_instances_of_one_dependent_class_freeze_different_capabilities`; `tests/test_operation_capability.py::test_a_dependent_instance_answers_nothing_before_it_finalizes`; `tests/test_operation_capability.py::test_the_base_constructor_never_generates_capabilities`; `tests/test_operation_capability.py::test_finalizing_a_second_time_is_refused_and_changes_nothing`; `tests/test_operation_capability.py::test_the_base_generator_is_unimplemented`; `tests/test_operation_capability.py::test_a_generator_that_raises_publishes_nothing`; `tests/test_operation_capability.py::test_an_invalid_generated_set_leaves_the_instance_unfinalized`; `tests/test_operation_capability.py::test_a_one_shot_generator_is_materialized_once`; `tests/test_operation_capability.py::test_a_snapshot_is_ordered_by_name_rather_than_by_generation_order`; `tests/test_operation_capability.py::test_a_snapshot_is_an_immutable_view_of_immutable_entries`; `tests/test_operation_capability.py::test_a_dependent_carrier_stays_open_to_further_subclassing`; `tests/test_operation_capability.py::test_instances_comparing_equal_keep_their_own_snapshots`; `tests/test_operation_capability.py::test_a_collected_dependent_carrier_leaves_no_snapshot_behind`; `tests/test_operation_capability.py::test_a_dependent_class_cannot_declare_class_capabilities`; `src/strideweave/carriers/_built_in_capabilities.py`; `tests/test_generic_reference.py::test_an_undeclared_plan_shape_is_refused`; `tests/test_generic_reference.py::test_a_float32_compute_never_stores_its_result_as_int32`; `tests/test_generic_reference.py::test_every_declared_capability_is_executable`; `tests/test_cpu_plan.py::test_an_accumulating_plan_without_an_accumulation_is_unsupported`; `tests/test_cpu_plan.py::test_an_injected_unsupported_plan_never_reaches_a_kernel`; `tests/test_cpu_plan.py::test_every_declared_capability_executes_on_a_real_tensor`; `tests/test_operation_capability.py::test_a_carrier_reports_its_backend_capabilities`; `tests/test_operation_capability.py::test_a_dependent_carrier_answers_for_its_instance_not_its_class`; `tests/test_operation_capability.py::test_a_dependent_carriers_public_answers_agree_with_each_other`; `tests/test_operation_capability.py::test_a_dependent_carrier_distinguishes_an_unknown_operation_from_a_shape`; `tests/test_operation_capability.py::test_an_unfinalized_dependent_carrier_answers_no_public_query`; `tests/test_operation_capability.py::test_an_independent_carrier_still_answers_from_its_exact_class`; `tests/test_operation_capability.py::test_a_custom_independent_carrier_answers_from_its_own_class`; `tests/test_operation_capability.py::test_a_carrier_query_is_asked_of_an_instance_rather_than_a_class`; `tests/test_evictable.py::test_a_hierarchy_advertises_what_its_primary_executes_and_it_can_keep`; `tests/test_evictable.py::test_a_hierarchy_drops_plans_whose_result_it_could_not_evict`; `tests/test_evictable.py::test_two_hierarchies_of_one_class_advertise_different_plans`; `tests/test_evictable.py::test_a_nested_hierarchy_composes_through_its_primary_instance_snapshot`; `tests/test_evictable.py::test_a_hierarchy_executes_every_plan_it_advertises`; `tests/test_evictable.py::test_an_unadvertised_plan_is_refused_before_any_work`; `tests/test_evictable.py::test_an_operand_shape_the_operation_does_not_accept_is_refused_at_the_gate`; `tests/test_evictable.py::test_an_operation_the_policy_does_not_describe_is_not_planned`; `tests/test_evictable.py::test_capabilities_are_structural_across_residency_and_release`; `tests/test_evictable.py::test_results_and_gradients_advertise_their_own_capabilities`; `tests/test_evictable.py::test_a_legacy_opaque_hierarchy_keeps_its_documented_behavior`; `tests/test_evictable.py::test_a_failed_capability_generation_leaves_no_owned_tiers`; `tests/test_operation_capability.py::test_evictable_has_no_class_declaration_to_seal`; `tools/lint_invariants.py`; `tests/test_lint_invariants.py`; `tests/test_operation_capability.py::test_capability_queries_do_not_filter_by_the_querying_carriers_dtype`; `tests/test_operation_capability.py::test_an_unknown_operation_is_distinguished_from_an_unsupported_plan`; `tests/test_operation_capability.py::test_a_custom_carrier_is_discoverable_without_altering_a_built_in`; `src/strideweave/carriers/generic/capabilities.py`; `src/strideweave/carriers/cpu/capabilities.py`; `tests/test_generic_reference.py`; `tests/test_dtype_conformance.py::test_generic_and_cpu_agree_on_every_registered_operation`; `tests/test_dtype_conformance.py::test_both_backends_produce_the_resolved_plan_output`; `tests/test_dtype_conformance.py::test_both_backends_attach_a_graph_exactly_when_the_plan_allows_one`; `tests/test_dtype_conformance.py::test_every_registered_operation_is_reachable_as_a_public_function`; `design/SimpleDType-operation-policy.md`; `src/strideweave/carriers/cpu/native/_cpu_policy.hpp` |

RT013 treats dependent-carrier finalization as a cooperative, single-threaded
construction step. The implementation calls
`_finalize_dependent_capabilities()` once, after installing its dependencies and
before the unfinished instance becomes reachable elsewhere. Concurrent
finalization and leaking a partially constructed instance are outside the
supported extension contract; the framework does not synchronize calls to the
extension-controlled generator. Snapshot publication remains failure-atomic,
and a later sequential finalization call is rejected.

## Native C++ Invariants

| ID | Invariant and canonical form | Enforcement | Evidence |
| --- | --- | --- | --- |
| `CPP001` | CPU hot loops release the Python GIL only after all required Python objects and cached layout metadata have been acquired. The released region must not call Python APIs. | Review | `py::gil_scoped_release` regions in `src/strideweave/carriers/cpu/native` |
| `CPP002` | Kernels iterate cached expanded layout keys and cached layout indices. They do not reconstruct hierarchical coordinates through Python inside hot loops. | Review | `_cpu_carrier.hpp`, `_cpu_operation.hpp`, `_cpu.cpp` |
| `CPP003` | Logical sizes, storage offsets, strides, and index arithmetic use signed `Index`. Container positions and lengths use `std::size_t`. | Build, Review | strict-warning build; native layout and carrier headers |
| `CPP004` | Crossings among Python sequence sizes, `std::size_t`, and signed `Index` use validated conversion helpers. Explicit casts are allowed only after range or non-negativity validation. | Build, Review | `layout_index::as_size`, `layout_index::size_as_index`; strict `-Wconversion` and `-Wsign-conversion` build |
| `CPP005` | Mutating native carrier entry points update carrier version state exactly as their Python-visible mutation contract requires. | Test, Review | native CPU mutation/scatter implementation; version tests |
| `CPP006` | Native operation registries store factories, not reusable operation objects; dispatch produces a fresh instance. | Test, Review | CPU operation registry and dispatch; `tests/test_cpu.py::test_cpu_dispatch_op_returns_supported_operations` |
| `CPP007` | Native code compiles cleanly with the project strict-warning set. Third-party pybind diagnostics receive only narrow, documented treatment; unfiltered `-Wpedantic` is not fatal. | Build | `STRIDEWEAVE_STRICT_WARNINGS` in `CMakeLists.txt`; `native-strict-warnings` CI job |
| `CPP008` | Every checked-in `.cpp` and `.hpp` conforms to the pinned project ClangFormat configuration. | Build | `.clang-format`; CI native-format step |
| `CPP009` | A Linux debug-oriented build runs the complete Python test suite with AddressSanitizer and UndefinedBehaviorSanitizer, halting on the first native diagnostic. | Build | `native-sanitizers` CI job; `STRIDEWEAVE_SANITIZERS` in `CMakeLists.txt` |

ThreadSanitizer is intentionally not an invariant check yet. Broad Clang-Tidy profiles
are also deferred. If introduced, evaluate diagnostic families individually and record
new enforced invariants here before making them fatal.

## Registry Maintenance

Use the existing prefix for the relevant layer (`SW`, `PY`, `RT`, or `CPP`) and the
next unused number. An entry must describe:

- the invariant in positive, implementation-guiding language;
- the canonical choice when multiple implementations are possible;
- every enforcement category currently providing coverage;
- stable evidence locations;
- any deliberately permitted exception.

Do not claim **Test** or **Build** enforcement unless a failing example would actually
make that check fail. Mark partially automated constraints as **Review** as well. When
removing an invariant, remove or revise its enforcement in the same change rather than
leaving a stale registry entry.
